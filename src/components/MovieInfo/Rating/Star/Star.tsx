import React from 'react'
import { TiStarFullOutline } from "react-icons/ti";
import { TiStarOutline } from "react-icons/ti";


type PropsType = {
    percentage: number,
};

export const Star: React.FC<PropsType> = ({percentage}) => {
    return (
        <div>
            
        </div>
    )
}
